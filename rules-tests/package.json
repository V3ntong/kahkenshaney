{
  "name": "rules-tests",
  "private": true,
  "description": "Security-rules tests run against the Firestore emulator (firebase emulators:exec --only firestore \"npm test\").",
  "scripts": {
    "test": "node --test rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1"
  }
}
