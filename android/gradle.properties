org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template.
# NOTE: Built-in Kotlin requires a Flutter version not yet on the stable
# channel (3.47+). Enabling it on Flutter 3.44.x breaks the build because
# Flutter tooling force-applies KGP to plugins like firebase_auth.
# Keep this disabled until Flutter is upgraded to 3.47+.
android.builtInKotlin=false
